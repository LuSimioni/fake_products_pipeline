
name: ecommerce_pipeline
version: '1.0.0'
config-version: 2

profile: ecommerce_pipeline

model-paths: ["models"]
test-paths: ["tests"]
log-path: "logs"
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ecommerce_pipeline:
    staging:
      +schema: STAGING
      +materialized: view
    marts:
      +schema: MARTS
      +materialized: table